# 服务端依赖（Python 3.10+）
# 安装：pip install -r requirements.txt

# --- Web 框架 ---
Flask==3.1.3

# --- 模型调用 ---
langchain==1.4.0
langchain-core==1.6.2
langchain-deepseek==1.1.0
langchain-openai==1.6.2
langchain-tavily==0.2.18   # Tavily 联网搜索工具
python-dotenv==1.2.3
rich==15.0.0

# --- MySQL ---
PyMySQL==1.2.0

# --- 附件解析（新增） ---
pypdf==6.18.0          # PDF 抽文本
python-docx==1.2.0     # Word(.docx) 抽段落与表格
openpyxl==3.1.5        # Excel(.xlsx) 转 Markdown 表格
pillow==12.3.0         # 超大图片压缩（可选，缺失时自动跳过压缩）

# --- HTTP 中间件说明 ---
# 请求 ID / 访问日志 / 安全头 / CORS / 限流 / Gzip / 统一错误 / 健康检查
# 全部用 Flask 钩子 + 标准库实现（见 http_middleware.py），不引入额外依赖。

# --- 生产部署时按需补充（本地 demo 不需要，故不写死在依赖里）---
# waitress            # Windows 友好的 WSGI 服务器：waitress-serve --listen=0.0.0.0:5000 app:app
# gunicorn            # Linux：gunicorn -w 1 -k gthread --threads 8 -b :5000 app:app
# flask-limiter       # 多实例部署时限流要换成共享存储（配合 redis）
# redis               # 会话检查点从 InMemorySaver 换成 Redis/Postgres，才能多 worker
